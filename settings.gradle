pluginManagement {
    plugins {
        id 'org.springframework.boot' version '3.1.5'
        id 'io.spring.dependency-management' version '1.1.3'
    }
}

rootProject.name = 'imshaby-api'

include 'server'
include 'common'
include 'meilisearch'
include 'meilisearch:api'